.navbar.navbar-fixed-top
    .navbar-inner
        .container
          a.btn.btn-navbar(data-target='.nav-collapse', data-toggle='collapse')
            span.icon-bar
            span.icon-bar
            span.icon-bar
  
          a.brand.serif(href='/')
            | OpinionWall
          .nav-collapse
            ul.nav.top
              li
                a(href='#', style="color:red") Beta                 
              li
                a(href='/surveys/new') #{__i('New Survey')}          
            
          if (req.isAuthenticated())
            ul.nav.pull-right              
              li.dropdown
                a.dropdown-toggle(data-toggle='dropdown', href='#')
                  | Account 
                  b.caret
                ul.dropdown-menu
                  li
                    a(href="/user/#{req.user.uid}") 
                      i.icon-user
                      |  #{__i('Me')}       
                  li.divider
                  li
                    a(href='/logout')           
                      i.icon-off 
                      |  #{__i('Logout')} 
                        
          else
            a.btn.btn-success.pull-right(href="/login", title="Login") #{__i('Login')}     
          .pull-right.btn-group.mrs
            form.form-search(method="get", action='/search')              
                input.span3(type="text", name="q", placeholder="Search")
                button.btn(type='submit') Go 
    if (req.param('back'))
      .alert.alert-success
        button.close(data-dismiss='alert') ×
        | #{__i('Please Login to proceed.')}                       